apiVersion: v1
kind: Service
metadata:
  name: eink-dashboard
  namespace: eink-dashboard
spec:
  selector:
    app: eink-dashboard
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
  type: ClusterIP
